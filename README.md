This is a students marks management project using Java ,Swing( for minimal UI), SQLite (for storing the students info).
Inside the code design pattern ( Singleton, Strategy, Factory, Command, Facade, Iterator, Memento, Observer, Composite, Adapter ) 
are implemented for better scalability and maintainability.
